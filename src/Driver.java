/**
 * Notes:
 * - fix login panel code, and maybe UI
 * - find better way to link panels/frames; I think it should come from a main frame ideally
 * - fix transactions panel
 * - priority is to fix the transactions panel
 */

import view.MainFrame;

public class Driver {
    public static void main(String[] args) throws Exception {
        /* call model, view, and controller here */
        MainFrame view = new MainFrame();
    }
}